pipeline {
    agent any 
	stages {
        stage('Stage 1') {
        	environment{
        	
        	}
		steps {
	                bat "echo running batch file : %date% : %time%"
	                bat "ls ${env.WORKSPACE}"



            }
        }
    }
}