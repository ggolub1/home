pipeline {
    agent any 
    stages {
        stage('Stage 1') {
            steps {
                echo "Hello world! : %date% : %time%"
                echo %WORKSPACE%
                echo "$WORKSPACE unix synt"
            }
        }
    }
}